@page
@model tpweb.Pages.Materias.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Materia</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Materia.IdMateria" />
            <div class="form-group">
                <label asp-for="Materia.Nombre" class="control-label"></label>
                <input asp-for="Materia.Nombre" class="form-control" />
                <span asp-validation-for="Materia.Nombre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Materia.CursoId" class="control-label"></label>
                <select asp-for="Materia.CursoId" class="form-control" asp-items="ViewBag.CursoId"></select>
                <span asp-validation-for="Materia.CursoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Materia.DocenteId" class="control-label"></label>
                <select asp-for="Materia.DocenteId" class="form-control" asp-items="ViewBag.DocenteId"></select>
                <span asp-validation-for="Materia.DocenteId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
