@page
@model tpweb.Pages.Materias.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Materia[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Materia[0].Curso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Materia[0].Docente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Materia) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curso.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Docente.IdUsuario)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdMateria">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdMateria">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdMateria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
