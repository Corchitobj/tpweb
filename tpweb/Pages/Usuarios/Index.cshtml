@page
@model tpweb.Pages.Usuarios.IndexModel

@{
    ViewData["Title"] = "Listado de Usuarios";
}

<h2 class="mb-4">Listado de Usuarios</h2>

<p>
    <a asp-page="Create" class="btn btn-primary mb-3">Registrar Usuario</a>
</p>

<table class="table table-bordered table-hover">
    <thead class="table-primary">
        <tr>
            <th>Apellido</th>
            <th>Nombre</th>
            <th>DNI</th>
            <th>Mail</th>
            <th>Usuario</th>
            <th>Rol</th>
            <th style="width: 180px;">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Usuario)
        {
            <tr>
                <td>@item.Apellido</td>
                <td>@item.Nombre</td>
                <td>@item.Dni</td>
                <td>@item.Mail</td>
                <td>@item.UsuarioNombre</td>
                <td>@item.Rol?.Nombre</td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.IdUsuario" class="btn btn-outline-warning btn-sm">Editar</a>
                    <a asp-page="./Details" asp-route-id="@item.IdUsuario" class="btn btn-outline-info btn-sm ms-1">Detalles</a>
                    <a asp-page="./Delete" asp-route-id="@item.IdUsuario" class="btn btn-outline-danger btn-sm ms-1">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
}